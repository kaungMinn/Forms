import { CustomizedDropDownTypes } from "./_types";

const CustomizedDropDown = (props: CustomizedDropDownTypes) => {
  const { theme } = props;
  const { inputColor } = theme;
  return <div></div>;
};

export default CustomizedDropDown;
